# Waitlist storage — paste an n8n (or any) webhook URL that accepts POST JSON:
# { email, stake_pick, stake_other, variant, utm_source, utm_medium, utm_content, page, ts }
# Leave empty for demo mode (signups log to browser console only).
VITE_WAITLIST_WEBHOOK_URL=
